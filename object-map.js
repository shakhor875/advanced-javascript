const student =[
    {id: 10, name:"omor sunny"},
    {id: 20, name:"romij ali"},
    {id: 30, name:"manna"},
    {id: 40, name:"dipjol"},
    {id: 50, name:"mahia"},
];

const names = student.map(s => s.name);
// const ids = student.map(s => s.id);

// const bigger =students.filter( s => s.id>40);
// const biggerOne = student.find( s => s.id >40);

console.log(names);